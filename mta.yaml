_schema-version: "3.2"
ID: hrbancodehorasgestor
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: hrbancodehorasgestor-destination-content
  type: com.sap.application.content
  requires:
  - name: hrbancodehorasgestor-destination-service
    parameters:
      content-target: true
  - name: hrbancodehorasgestor-repo-host
    parameters:
      service-key:
        name: hrbancodehorasgestor-repo-host-key
  - name: hrbancodehorasgestor-uaa
    parameters:
      service-key:
        name: hrbancodehorasgestor-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: hrbancodehorasgestor_html_repo_host
          ServiceInstanceName: hrbancodehorasgestor-html5-srv
          ServiceKeyName: hrbancodehorasgestor-repo-host-key
          sap.cloud.service: hrbancodehorasgestor
        - Authentication: OAuth2UserTokenExchange
          Name: hrbancodehorasgestor_uaa
          ServiceInstanceName: hrbancodehorasgestor-xsuaa-srv
          ServiceKeyName: hrbancodehorasgestor-uaa-key
          sap.cloud.service: hrbancodehorasgestor
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: hrbancodehorasgestor-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: hrbancodehorasgestor-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - hrbancodehorasgestor.zip
      name: hrbancodehorasgestor
      target-path: resources/
- name: hrbancodehorasgestor
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: hrbancodehorasgestor-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: hrbancodehorasgestor-destination-service
    service-plan: lite
- name: hrbancodehorasgestor-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: hrbancodehorasgestor-xsuaa-srv
    service-plan: application
- name: hrbancodehorasgestor-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: hrbancodehorasgestor-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
